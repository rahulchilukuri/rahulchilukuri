services:
#    postgres:
#      image: 'postgres:17-alpine'
#      ports:
#        - '5432:5432'
#      environment:
#        - 'POSTGRES_USER=myuser'
#        - 'POSTGRES_DB=mydatabase'
#        - 'POSTGRES_PASSWORD=secret'
#      volumes:
#        - ~/database/postgres/data:/var/lib/postgresql/data
#        - ~/database/init-scripts:/docker-entrypoint-initdb.d

#  cassandra:
#    image: cassandra:latest
#    container_name: cassandra
#    environment:
#      - CASSANDRA_CLUSTER_NAME=ssl-cluster
#    volumes:
#      - ~/database/cassandra/data:/var/lib/cassandra
#    #      - ~/database/cassandra/ssl:/etc/cassandra/ssl
#    ports:
#      - "9042:9042"

#  neo4j:
#    image: neo4j:latest
#    environment:
#      - NEO4J_AUTH=none
#    ports:
#      - "7474:7474"
#      - "7687:7687"
#    volumes:
#      - ~/database/neo4j/data:/data
##      - ~/database/neo4j/logs:/logs
#  couchdb:
#    image: couchdb
#    restart: always
#    ports:
#      - "5984:5984"
#    environment:
#      - COUCHDB_USER=admin
#      - COUCHDB_PASSWORD=password
#    volumes:
#      - ~/database/couchdb/data:/opt/couchdb/data

#  mongo:
#    image: mongo
#    restart: always
#    environment:
#      MONGO_INITDB_ROOT_USERNAME: root
#      MONGO_INITDB_ROOT_PASSWORD: example
#
#  mongo-express:
#    image: mongo-express
#    restart: always
#    ports:
#      - 8081:8081
#    environment:
#      ME_CONFIG_MONGODB_ADMINUSERNAME: root
#      ME_CONFIG_MONGODB_ADMINPASSWORD: example
#      ME_CONFIG_MONGODB_URL: mongodb://root:example@mongo:27017/
#      ME_CONFIG_BASICAUTH: false

  prometheus:
    image: prom/prometheus
    volumes:
#      - ./prometheus/:/etc/prometheus/
      - prometheus_data:/prometheus
#    command:
#      - '--config.file=/etc/prometheus/prometheus.yml'
#      - '--storage.tsdb.path=/prometheus'
    ports:
      - 9090:9090
    networks:
      - prometheus-net
    restart: always
  node-exporter:
      image: prom/node-exporter:latest
      volumes:
        - /proc:/host/proc:ro
        - /sys:/host/sys:ro
        - /:/rootfs:ro
      command:
        - '--path.rootfs=/host'
        - '--path.procfs=/host/proc'
        - '--path.sysfs=/host/sys'
        - --collector.filesystem.ignored-mount-points
        - "^/(sys|proc|dev|host|etc|rootfs/var/lib/docker/containers|rootfs/var/lib/docker/overlay2|rootfs/run/docker/netns|rootfs/var/lib/docker/aufs)($$|/)"
      ports:
        - 9100:9100
      networks:
        - prometheus-net
      restart: always
#  opensearch-node1:
#    image: opensearchproject/opensearch:latest
#    container_name: opensearch-node1
#    platform: linux/amd64
#    environment:
#      - cluster.name=opensearch-cluster
#      - node.name=opensearch-node1
#      - discovery.seed_hosts=opensearch-node1,opensearch-node2
#      - cluster.initial_cluster_manager_nodes=opensearch-node1,opensearch-node2
#      - bootstrap.memory_lock=true  # along with the memlock settings below, disables swapping
#      - OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m  # minimum and maximum Java heap size, recommend setting both to 50% of system RAM
#      - OPENSEARCH_INITIAL_ADMIN_PASSWORD=anfwyc@87!gdf$VWAFL(WS&*
#      # Sets the demo admin user password when using demo configuration, required for OpenSearch 2.12 and higher
#    ulimits:
#      memlock:
#        soft: -1
#        hard: -1
#      nofile:
#        soft: 65536  # maximum number of open files for the OpenSearch user, set to at least 65536 on modern systems
#        hard: 65536
#    volumes:
#      - opensearch-data1:/usr/share/opensearch/data
#    ports:
#      - 9200:9200
#      - 9600:9600  # required for Performance Analyzer
#    networks:
#      - opensearch-net
#  opensearch-node2:
#      image: opensearchproject/opensearch:latest
#      container_name: opensearch-node2
#      platform: linux/amd64
#      environment:
#        - cluster.name=opensearch-cluster
#        - node.name=opensearch-node2
#        - discovery.seed_hosts=opensearch-node1,opensearch-node2
#        - cluster.initial_cluster_manager_nodes=opensearch-node1,opensearch-node2
#        - bootstrap.memory_lock=true
#        - OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m
#        - OPENSEARCH_INITIAL_ADMIN_PASSWORD=anfwyc@87!gdf$VWAFL(WS&*
#      ulimits:
#        memlock:
#          soft: -1
#          hard: -1
#        nofile:
#          soft: 65536
#          hard: 65536
#      volumes:
#        - opensearch-data2:/usr/share/opensearch/data
#      networks:
#        - opensearch-net
#  opensearch-dashboards:
#      image: opensearchproject/opensearch-dashboards:latest
#      container_name: opensearch-dashboards
#      ports:
#        - 5601:5601
#      expose:
#        - '5601'
#      environment:
#        OPENSEARCH_HOSTS: '["opensearch-node1:9200","opensearch-node2:9200"]'
#      networks:
#        - opensearch-net

volumes:
  opensearch-data1:
  opensearch-data2:
  prometheus_data: {}

networks:
  opensearch-net:
  prometheus-net:
