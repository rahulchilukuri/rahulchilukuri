# LeetCode Interview Keyword → Data Structure Cheat Sheet

Quick-reference: when a problem statement uses these words, reach for the matching structure/pattern first.

## Array
index, sliding window, two pointer, subarray, prefix sum, k element, in place, monotonic, sorted array, palindrome, pair sum, remove duplicates, partition, merge, container, trapping, reverse in place, at most k, longest/shortest, contiguous, distinct characters, max/min in range, k consecutive

## HashMap / HashSet
frequency (count), seen, visited, memoize (cache), group by, anagram, two sum, complement, index lookup, first unique, isomorphic, dedupe

## Stack
undo, balanced bracket, reverse, backtrack, DFS, call stack, monotonic stack, next greater/smaller, matching, nested, decode, evaluate expression, histogram

## Queue / Deque
BFS, level order, shortest path by level, schedule, sliding window, monotonic, rotting, task scheduler, circular buffer, first-in-first-out, recent calls

## Linked List
reverse, cycle detection, fast/slow pointers, two pointers, middle element, merge, dummy head, palindrome, nth from end, intersection, flatten, copy with random pointer

## Tree (Binary Tree / BST)
hierarchy, parent, depth, recursion, DFS, in-order, pre-order, post-order, least common ancestor, balanced, traversal, path sum, diameter, symmetric, serialize/deserialize, kth smallest, floor/ceiling, range sum

## Heap / Priority Queue
top k, k largest/smallest, priority, median stream (running median), merge k, closest points, scheduling, frequency sort

## Graph
network, connection, path, root, islands, component, cycle, dependency, topological, shortest, clone, bipartite, matrix traversal, flood fill

## Union-Find
connected, redundant connection, accounts merge, provinces, cycle in undirected graph, network connectivity

## Trie
prefix search, autocomplete, suggest, starts with, dictionary word, longest prefix, word break, XOR

## Binary Search
sorted, rotated, search insert position, peak element, first/last occurrence, minimize maximum, capacity/speed, guess number

## Backtracking
permutations, combinations, subsets, N-Queens, Sudoku, partition, letter combinations, generate parentheses, word search

## Dynamic Programming
climbing, robber, coin change, longest common/increasing, edit distance, knapsack, jump game, unique paths, palindromic substring, buy/sell stock

## Greedy
interval scheduling, jump game, gas station, candy distribution, task scheduling, minimum platforms

## Bit Manipulation
single number, missing number, power of two, counting bits, XOR, subsets via bitmask

## Matrix / 2D Grid
rotate, spiral, transpose, flood fill, number of islands, connected region, in-place modification, diagonal traversal

## String
substring, anagram, palindrome, rotation, pattern matching, valid parentheses variant, run-length, longest common prefix, string builder/mutation

## Divide & Conquer
merge sort, quick select, closest pair, majority element, maximum subarray (Kadane's variant), split and combine

## Recursion / Memoization
recurrence relation, base case, top-down, bottom-up, overlapping subproblems, optimal substructure

## Design
LRU cache, LFU cache, min stack, design hashmap, design twitter/feed, iterator, rate limiter, autocomplete system

## Math / Number Theory
GCD/LCM, prime sieve, modular arithmetic, digit manipulation, power/exponentiation, combinatorics, probability

## Intervals
merge intervals, insert interval, meeting rooms, non-overlapping, interval scheduling, sweep line
